Not satisfied yet. Source review of `61c5a32` confirms the atomic no-overwrite fix, per-receipt coverage, destination scopes, and import inventory ordering. These blockers remain:

1. **P1 — Overflow silently loses lifecycle events.** [macos/files.rs:33](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/files.rs:33) drops the event, then attempts `Overflow` on the same full queue. There is no later notification retry. Losing `Cancel`, `DragEnded`, `PromiseFinished`, or `ClipboardFiles` loses cancellation, completion, or clipboard invalidation. Most callers ignore rejection; drag startup proceeds even if `DragStarted` was rejected. **Fix:** preserve terminal events and clipboard invalidation in bounded pending state, deliver overflow reliably when capacity returns, and refuse native startup when admission fails.

2. **P1 — Dropping the parent’s writer no longer resolves the native callback.** [promise.rs:105](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/promise.rs:105) retains a `PromiseWriter` locally while blocking on its receiver. Consequently, dropping every parent-owned writer cannot run `PromiseWriterInner::drop` or disconnect the channel. Four abandoned requests can occupy all promise workers indefinitely. **Fix:** separate the cancellation token from reply-sender ownership; move the sole reply endpoint into the event and retain only cancellation state locally.

3. **P1 — Two minutes does not establish that a local drag’s reader finished.** [promise.rs:283](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/promise.rs:283) releases the independent receipt pin after 120 seconds. A destination copying a large directory can still be opening children when Clear becomes possible and deletes them. **Fix:** retain exported receipts until an explicit release policy permits invalidating external URLs. Use completion-backed promises where available; elapsed time cannot substitute for consumer completion.

4. **P1 — Cancellation during final sync still publishes successfully.** [promise.rs:185](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/promise.rs:185) can block in `sync_all`; afterward [promise.rs:159](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/promise.rs:159) renames without another cancellation check. Cancelling during sync therefore produces a successful destination. **Fix:** add a shared cancellation/publication gate after sync. Cancellation winning that gate must remove staging; publication winning it establishes the commit point.

5. **P2 — Clear retains the adapter’s own pin.** [shelf.rs:486](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/shelf.rs:486) emits `ClearReceived` while its row still owns `DragMode::LocalUrls(ReceivedSelection)`. Dropping only the parent’s pin, as documented, cannot make core clearing succeed. **Fix:** acknowledge removal of idle presentation pins before requesting core clear, and prevent snapshots from repinning during that operation. Keep actual clipboard and drag pins independent.

6. **P2 — Provider retirement breaks late callbacks and does not distinguish active writes.** [promise.rs:310](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/promise.rs:310) retires every incomplete attempt after ten minutes, including writes already running. After 64 subsequently retained attempts, its delegates are dropped. The SDK declares the provider’s delegate **weak**, so a still-held provider can lose its callback target. **Fix:** give each provider strong delegate ownership through `userInfo` or an equivalent carrier, retain active callbacks through completion, and bound admission rather than evicting live ownership.

7. **P2 — Unfinished imports still leak staging and have no terminal result.** [shelf.rs:890](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/shelf.rs:890) only removes receivers after thirty minutes. Cleanup ownership is created exclusively on successful completion at [shelf.rs:944](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/shelf.rs:944). An import that never supplies all callbacks leaves its directory behind. Completed receivers also remain registered until the timer. **Fix:** create the staging guard immediately, share it with pending callbacks, remove completed registry entries promptly, and make abandonment an explicit terminal error while preserving ownership for writers still running. Add an import admission limit.

8. **P2 — Splice’s own text/image publication leaves an obsolete file pin held.** [pasteboard.rs:97](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/pasteboard.rs:97) skips self-written generations before calling `release_publication_if_replaced`. Replacing received-file URLs through `set_remote_offer` therefore keeps the old receipt pinned until another external clipboard change. **Fix:** release obsolete publication ownership before the poller’s self-write/unchanged early return.

9. **P2 — Receive intent allocation is outside the publication lock.** [macos/files.rs:49](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/files.rs:49) can advance the intent after an older publication passes its check at line 131 but before it clears the clipboard. The older result then publishes despite the newer click; the newer result can subsequently fail its generation check. **Fix:** serialize intent allocation and generation capture with the same lock covering publication.

The import callback-before-inventory case now waits correctly and completes once. Progress updates respect Hide, and outgoing drags defer structural row/layout changes. One bounded UI exception remains: [shelf.rs:1283](/Users/daniel/Work/splice-file-work/macos/crates/splice-platform/src/macos/shelf.rs:1283) unconditionally shows asynchronous status errors even after Hide; honor `user_hidden` there too if the documented behavior is intended.

No files changed and no tests, Finder operations, pilots, UI creation, or input actions ran. Native acceptance remains for the separate interactive session.
